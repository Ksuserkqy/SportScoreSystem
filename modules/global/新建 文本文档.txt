fetch("../update/version/latest.json")
  .then(function(response) {
    return response.json();
  })
  .then(function(jsonData) {
    // 使用jsonData进行后续操作
    console.log(jsonData["offline-cmd"]);
  })
  .catch(function(error) {
    console.error('Error:', error);
  });